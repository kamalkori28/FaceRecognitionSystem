import ReactDOM from "react-dom/client";

import { RootProviders } from "@/app/providers";
import "@/styles/globals.css";

ReactDOM.createRoot(document.getElementById("root")!).render(<RootProviders />);

